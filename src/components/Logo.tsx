import { Component, ReactNode } from "react";
import "../styles.css";

export class Logo extends Component {
    render(): ReactNode {
        return (
            <div className="logo" />
        );
    }
}